It is a program which applies distortion effect to the live webcam feed.
In this, firstly the Cartesian coordinates of pixels are converted to polar coordinates.
A new distorted radius is calculated using a equation.
Then again the new polar coordinates are converted back to Cartesian coordinates.
